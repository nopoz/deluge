setuptools < 82
libtorrent
twisted[tls]>=17.1
rencode
# 26 removed crypto.X509Req, which crypto_utils.py still uses, so the daemon
# fails to start. Upstream PR #514 fixes the code; drop this pin when it lands.
pyopenssl < 26
pyxdg
pillow
mako
setuptools
chardet
setproctitle
pywin32; sys_platform == 'win32'
certifi; sys_platform == 'win32'
windows-curses; sys_platform == 'win32'
zope.interface>=4.4.2,<8.0
distro; 'linux' in sys_platform or 'bsd' in sys_platform
pygeoip
ifaddr>=0.2.0
